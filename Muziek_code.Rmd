---
title: "Muziek Database"
author: "Connor Whalen"
date: "2024-07-12"
output:
  html_document:
    theme: readable
    highlight: textmate
    toc: true
    toc_float: true
    toc_depth: 3
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

# Welcome to the Muziek Spreadsheet
You are probably here because you want to listen to some more music. Maybe you are here because you want to see if your taste is better than your peers. NEWSFLASH: it isn't! So the real reason you are here then is to kneel at the feet of me and mine, send your condolences to you and yours, and open those ears up to what is right. Happy listening! 

#### Install packages. 
```{r packages.i, eval=FALSE, echo=FALSE}
install.packages("tidyverse")
install.packages("janitor")
install.packages("ggeffects")
install.packages("ggpubr")
install.packages("googlesheets4")
```

#### Load packages. 
```{r packages.l, echo=TRUE}
library(googlesheets4)
library(ggpubr)
library(ggeffects)
library(tidyverse)
library(readr)
library(dbplyr)
library(lubridate)
library(ggplot2)
```

## Instructions to load database
Load spreadsheet data using gs4 (pull directly fromt the sheet itself).
```{r gs4 load, include=FALSE, eval=FALSE}
gs4_auth(scopes = "spreadsheets.readonly") 
# muziekspreadsheet@gmail.com <-- sign in when prompted
# muziekspreadsheet3@Tm3H@cR <-- password for google account
muziek_main <- read_sheet("https://docs.google.com/spreadsheets/d/1QH27wRqhg-6MS9vdQt7C73dLpHvsv3nov9YgxcZOlJQ/edit?pli=1&gid=0#gid=0") %>%
  janitor::clean_names()
```

### Plots 
```{r}
c_ry <- ggplot(muziek_main,
       aes(x = year, y = rating)) + geom_point() + xlab("Year")  + 
  ylab("Rating")  + theme_bw() + geom_smooth(method = lm)

a_ry <-  ggplot(muziek_main,
       aes(x = year, y = a_rating)) + geom_point() + xlab("Year")  + 
  ylab("Rating")  + theme_bw() + geom_smooth(method = lm)

ggarrange(c_ry, a_ry, common.legend = TRUE, legend = "bottom", labels = c("A", "B"))

c_rg <- ggplot(muziek_main,
       aes(x = gender, y = rating)) + geom_boxplot() + xlab("Gender")  + 
  ylab("Rating")  + theme_bw() + geom_smooth(method = lm)

a_rg <-  ggplot(muziek_main,
       aes(x = gender, y = a_rating)) + geom_boxplot() + xlab("Gender")  + 
  ylab("Rating")  + theme_bw() + geom_smooth(method = lm)

ggarrange(c_rg, a_rg, common.legend = TRUE, legend = "bottom", labels = c("A", "B"))

c_rg2 <- ggplot(muziek_main,
       aes(x = genre, y = rating)) + geom_boxplot() + xlab("Genre")  + 
  ylab("Rating")  + theme_bw() + geom_smooth(method = lm)

a_rg2 <-  ggplot(muziek_main,
       aes(x = genre, y = a_rating)) + geom_boxplot() + xlab("Genre")  + 
  ylab("Rating")  + theme_bw() + geom_smooth(method = lm)

ggarrange(c_rg2, a_rg2, common.legend = TRUE, legend = "bottom", labels = c("A", "B"))

c_rl <- ggplot(muziek_main,
       aes(x = general_location, y = rating)) + geom_boxplot() + xlab("Genre")  + 
  ylab("Rating")  + theme_bw() + geom_smooth(method = lm)

a_rl <-  ggplot(muziek_main,
       aes(x = general_location, y = a_rating)) + geom_boxplot() + xlab("Genre")  + 
  ylab("Rating")  + theme_bw() + geom_smooth(method = lm)

ggarrange(c_rl, a_rl, common.legend = TRUE, legend = "bottom", labels = c("A", "B"))

ggplot(muziek_main,
       aes(x = year, y = runtime_min)) + geom_point() + xlab("Year")  + 
  ylab("Runtime")  + theme_bw() + geom_smooth(method = lm)

```




